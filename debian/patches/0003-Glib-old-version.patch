From: Andrea Zagli <azagli@libero.it>
Date: Sun, 20 Nov 2016 17:11:55 +0100
Subject: Glib old version.

---
 configure.ac    | 8 ++++----
 libzakcgi.pc.in | 2 +-
 2 files changed, 5 insertions(+), 5 deletions(-)

diff --git a/configure.ac b/configure.ac
index df5a82b..ab4e67d 100644
--- a/configure.ac
+++ b/configure.ac
@@ -30,10 +30,10 @@ AC_PROG_LIBTOOL
 AC_PROG_RANLIB
 
 # Checks for libraries.
-PKG_CHECK_MODULES(ZAKCGI, [glib-2.0 >= 2.36
-                           gobject-2.0 >= 2.36
-                           gio-2.0 >= 2.36
-                           gio-unix-2.0 >= 2.36])
+PKG_CHECK_MODULES(ZAKCGI, [glib-2.0 >= 2.32
+                           gobject-2.0 >= 2.32
+                           gio-2.0 >= 2.32
+                           gio-unix-2.0 >= 2.32])
 
 AC_SUBST(ZAKCGI_CFLAGS)
 AC_SUBST(ZAKCGI_LIBS)
diff --git a/libzakcgi.pc.in b/libzakcgi.pc.in
index 8711cfb..8135ac7 100644
--- a/libzakcgi.pc.in
+++ b/libzakcgi.pc.in
@@ -6,6 +6,6 @@ includedir=@includedir@
 Name: @PACKAGE_NAME@
 Description: GObject class to implement CGI softwares.
 Version: @PACKAGE_VERSION@
-Requires: glib-2.0 >= 2.36 gobject-2.0 gio-2.0 gio-unix-2.0
+Requires: glib-2.0 >= 2.32 gobject-2.0 gio-2.0 gio-unix-2.0
 Libs: -L${libdir} -lzakcgi
 Cflags: -I${includedir}
